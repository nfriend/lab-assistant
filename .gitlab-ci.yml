image: node:latest

stages:
  - lint
  - build
  - test
  - deploy

# Cache npm modules in between jobs
cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - Lab_Assistant/lambda/custom/node_modules/

before_script:
  - cd Lab_Assistant/lambda/custom
  - npm install

lint code:
  stage: lint
  script:
    - npm run lint

lint translations:
  stage: lint
  script:
    - npm run translate
    - git diff-index --quiet HEAD -- || echo "New text has been added! Please run \"npm run translate\" and commit these changes."

build:
  stage: build
  script:
    - npm run build

jest:
  stage: test
  script:
    - npm run test
  artifacts:
    paths:
      - Lab_Assistant/lambda/custom/test-report.xml
    reports:
      junit: Lab_Assistant/lambda/custom/test-report.xml

deploy:
  stage: deploy
  script:
    - npm run deploy
  only:
    - deploy@nfriend/lab-assistant
